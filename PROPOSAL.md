# Animated Flow Diagram Tech Blog - 구현 제안서

> **Version**: 1.1 (2025-11-26)
> **Status**: Draft → Review

## 1. 비전 및 목표

### 1.1 핵심 비전
**"스크립트/정의 기반 애니메이션 Flow Diagram"** - 복잡한 기술 개념을 동적 시각화로 쉽게 전달하는 차세대 기술 블로그 플랫폼

### 1.2 기존 방식의 한계

| 방식 | 한계점 | AnimFlow 해결책 |
|------|--------|-----------------|
| 텍스트/이미지 | 정적, 복잡한 흐름 표현 어려움 | 애니메이션 시퀀스로 단계별 설명 |
| Mermaid/PlantUML | 정적 다이어그램, 인터랙션 없음 | 시나리오 기반 인터랙티브 제어 |
| 동영상 | 수정 어려움, 용량 큼, 인터랙션 불가 | 선언적 DSL로 쉬운 수정, 경량 |
| 커스텀 Canvas/SVG | 코드 작성 필요, 재사용성 낮음 | DSL 표준화로 재사용 가능 |

### 1.3 목표
1. **선언적 DSL (Domain Specific Language)** 설계
2. **플러그인 에코시스템** 구축
3. **표준 포맷** 제정 및 오픈소스화
4. **기술 블로그** 운영을 통한 실증

### 1.4 성공 지표 (KPIs)
| 지표 | 목표 (6개월) | 목표 (12개월) |
|------|-------------|---------------|
| DSL 스펙 완성도 | v1.0 릴리즈 | v1.5 (확장 기능) |
| 블로그 포스트 | 10개 | 30개 |
| GitHub Stars | 100+ | 500+ |
| NPM 다운로드 | 500/월 | 2,000/월 |

---

## 2. 시스템 아키텍처

### 2.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Tech Blog Platform                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │                         User Interface Layer                             ││
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    ││
│  │  │   Editor    │  │  Controls   │  │   Stats     │  │    Logs     │    ││
│  │  │  (Monaco)   │  │  (Buttons)  │  │   Panel     │  │   Panel     │    ││
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘    ││
│  └─────────┼────────────────┼────────────────┼────────────────┼───────────┘│
│            │                │                │                │             │
│            ▼                ▼                ▼                ▼             │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │                      AnimFlow Core Engine                                ││
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐││
│  │  │  Parser   │ │   Node    │ │   Edge    │ │ Animation │ │ Scenario  │││
│  │  │  (YAML/   │→│  Manager  │→│  Manager  │→│  Engine   │→│  Runner   │││
│  │  │   JSON)   │ │           │ │           │ │           │ │           │││
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘ └───────────┘││
│  │        ↓                                                               ││
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐                            ││
│  │  │  Schema   │ │   Event   │ │   State   │                            ││
│  │  │ Validator │ │   Bus     │ │   Store   │                            ││
│  │  └───────────┘ └───────────┘ └───────────┘                            ││
│  └─────────────────────────────────────────────────────────────────────────┘│
│            │                                                                 │
│            ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │                        Renderer Layer                                    ││
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         ││
│  │  │  Canvas 2D      │  │  SVG (접근성)    │  │  WebGL (고성능)  │         ││
│  │  │  (기본 렌더러)   │  │  (선택적)        │  │  (선택적)        │         ││
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘         ││
│  └─────────────────────────────────────────────────────────────────────────┘│
│            │                                                                 │
│            ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │                    Integration Layer                                     ││
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    ││
│  │  │   Astro     │  │  Next.js    │  │    Vue      │  │  Vanilla    │    ││
│  │  │  Plugin     │  │  Component  │  │  Component  │  │    JS       │    ││
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    ││
│  └─────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 데이터 흐름

```
.animflow.yaml  →  Parser  →  AST  →  Validator  →  Runtime State
                                                          │
                    ┌─────────────────────────────────────┘
                    ▼
              Event Bus  ←→  User Interaction
                    │
                    ▼
              Animation Engine  →  Frame Updates  →  Renderer  →  Canvas
```

### 2.3 핵심 모듈 설명

| 모듈 | 책임 | 주요 기능 |
|------|------|----------|
| **Parser** | DSL 해석 | YAML/JSON → AST 변환, 문법 검증 |
| **Validator** | 스키마 검증 | JSON Schema 기반 유효성 검사 |
| **Node Manager** | 노드 관리 | 노드 CRUD, 위치 계산, 스타일 적용 |
| **Edge Manager** | 연결선 관리 | 경로 계산, 화살표 렌더링 |
| **Animation Engine** | 애니메이션 | 타이밍, 이징, 프레임 관리 |
| **Scenario Runner** | 시나리오 실행 | 스텝 순차 실행, 조건 분기 |
| **Event Bus** | 이벤트 통신 | 컴포넌트 간 느슨한 결합 |
| **State Store** | 상태 관리 | 현재 상태, 통계, 로그 관리 |

---

## 3. AnimFlow DSL 설계

### 3.1 파일 포맷: `.animflow.yaml` 또는 `.animflow.json`

```yaml
# example.animflow.yaml
version: "1.0"
metadata:
  title: "LDAP + Keycloak + OPA 캐싱 플로우"
  author: "Your Name"
  description: "멀티 레이어 캐싱 시스템 시각화"
  tags: ["caching", "authentication", "authorization"]

canvas:
  width: 1400
  height: 800
  background: "#f8f9fa"
  sections:  # 캔버스 영역 구분 (선택적)
    - id: auth-layer
      label: "인증 레이어 (Authentication)"
      bounds: { y: 0, height: 380 }
      style: { background: "#f8f9fa", labelColor: "#1f4788" }
    - id: authz-layer
      label: "인가 레이어 (Authorization)"
      bounds: { y: 380, height: 420 }
      style: { background: "#fafafa", labelColor: "#0284c7" }

# 노드 정의
nodes:
  - id: client
    type: box
    label: "Client"
    position: { x: 100, y: 150 }
    section: auth-layer
    style:
      color: "#3b82f6"
      shape: rounded-rect
      width: 120
      height: 60

  - id: go-backend
    type: box
    label: "Go Backend"
    position: { x: 250, y: 150 }
    section: auth-layer
    style:
      color: "#3b82f6"

  - id: gocache
    type: box
    label: "Gocache\n(L1)"
    position: { x: 400, y: 150 }
    section: auth-layer
    style:
      color: "#f59e0b"
      icon: "cache"

# 연결선 정의
edges:
  - id: e1
    from: client
    to: go-backend
    label: "① Token"
    style:
      color: "#adb5bd"
      lineType: solid  # solid, dashed, dotted
      animated: false

  - id: e2
    from: go-backend
    to: gocache
    label: "② Check"
    style:
      color: "#adb5bd"

# 변수 정의 (시나리오에서 사용)
variables:
  jwtCacheHit: { type: boolean, default: false }
  infinispanHit: { type: boolean, default: false }
  totalTime: { type: number, default: 0 }

# 애니메이션 시나리오
scenarios:
  - id: cache-hit
    name: "전체 캐시 히트"
    description: "모든 레이어에서 캐시 히트"
    init:  # 시나리오 시작 시 변수 초기화
      jwtCacheHit: true
      infinispanHit: true
    steps:
      - action: highlight
        nodes: [client, go-backend]
        edge: e1
        label: "HTTP Request + Token"
        duration: 1000
        log: { message: "① HTTP 요청 시작", type: "info" }

      - action: animate-edge
        edge: e2
        style: { color: "#10b981", glow: true }
        label: "✓ Cache Hit!"
        duration: 800
        log: { message: "캐시 히트!", type: "success" }
        stats:
          jwt-time: 0.1

  - id: cache-miss
    name: "캐시 미스"
    init:
      jwtCacheHit: false
    steps:
      - action: highlight
        nodes: [gocache, keycloak]
        style: { color: "#ef4444" }
        label: "✗ Cache Miss"
        log: { message: "캐시 미스 - Keycloak 조회", type: "warning" }

  - id: random
    name: "랜덤 시뮬레이션"
    description: "실제 환경을 시뮬레이션"
    init:
      jwtCacheHit: { $random: { probability: 0.8 } }  # 80% 확률로 true
      infinispanHit: { $random: { probability: 0.7 } }
    steps:
      - action: conditional
        condition: { $var: jwtCacheHit }
        then:
          - action: animate-edge
            edge: e2
            style: { color: "#10b981" }
            label: "✓ Cache Hit!"
        else:
          - action: animate-edge
            edge: e2
            style: { color: "#ef4444" }
            label: "✗ Cache Miss"
          - action: goto
            scenario: cache-miss-flow

# 컨트롤 UI 정의
controls:
  scenarios:
    type: button-group
    options:
      - { id: cache-hit, label: "✅ 전체 캐시", default: false }
      - { id: cache-miss, label: "❌ 캐시 없음" }
      - { id: random, label: "🎲 랜덤", default: true }

  speed:
    type: select
    label: "애니메이션 속도"
    options:
      - { value: 2000, label: "느림" }
      - { value: 1000, label: "보통", default: true }
      - { value: 500, label: "빠름" }
      - { value: 200, label: "매우 빠름" }

  buttons:
    - { id: start, label: "▶ 시뮬레이션 시작", action: start }
    - { id: reset, label: "↻ 리셋", action: reset }

# 통계 패널 정의
stats:
  - id: jwt-time
    label: "JWT 검증 시간"
    unit: "ms"
    format: "0.00"  # 소수점 2자리
  - id: opa-time
    label: "OPA 평가 시간"
    unit: "ms"
  - id: total-time
    label: "총 응답 시간"
    unit: "ms"
    compute: { $add: [jwt-time, opa-time, 1.85] }  # 계산식
  - id: hit-rate
    label: "캐시 히트율"
    unit: "%"
    format: "0"

# 로그 패널 설정
logging:
  enabled: true
  maxEntries: 20
  timestampFormat: "HH:mm:ss"
  styles:
    info: { color: "#3b82f6", icon: "ℹ️" }
    success: { color: "#10b981", icon: "✓" }
    warning: { color: "#f59e0b", icon: "⚠️" }
    error: { color: "#ef4444", icon: "✗" }
```

### 3.2 기존 HTML → AnimFlow DSL 매핑 예시

기존 `caching-flow-diagram.html`의 JavaScript 코드가 DSL로 어떻게 변환되는지:

**기존 JavaScript:**
```javascript
const nodes = {
    client: { x: 100, y: 150, label: 'Client', color: '#3b82f6' },
    goBackend: { x: 250, y: 150, label: 'Go Backend', color: '#3b82f6' },
    // ...
};
```

**AnimFlow DSL:**
```yaml
nodes:
  - id: client
    label: "Client"
    position: { x: 100, y: 150 }
    style: { color: "#3b82f6" }
  - id: go-backend
    label: "Go Backend"
    position: { x: 250, y: 150 }
    style: { color: "#3b82f6" }
```

**기존 JavaScript (애니메이션):**
```javascript
async function animateStep(from, to, color, label, type = 'info') {
    drawStaticDiagram();
    drawNode(from, true);
    drawNode(to, true);
    drawArrow(from, to, color, label, false, true);
    addLog(label, type);
    await sleep(animationSpeed);
}
```

**AnimFlow DSL:**
```yaml
steps:
  - action: animate-edge
    from: client
    to: go-backend
    style: { color: "#3b82f6", glow: true }
    label: "HTTP Request"
    duration: { $var: animationSpeed }
    log: { message: "HTTP Request", type: "info" }
```

### 3.3 DSL 핵심 개념

| 개념 | 설명 | 예시 |
|------|------|------|
| **Node** | 다이어그램의 구성 요소 | box, circle, icon, group, database |
| **Edge** | 노드 간 연결선 | solid, dashed, animated |
| **Section** | 캔버스 영역 구분 | auth-layer, authz-layer |
| **Scenario** | 애니메이션 시나리오 | cache-hit, cache-miss |
| **Step** | 개별 애니메이션 액션 | highlight, animate-edge, delay |
| **Variable** | 런타임 변수 | boolean, number, string |
| **Control** | 사용자 인터랙션 요소 | button-group, slider, select |
| **Stats** | 실시간 통계 표시 | jwt-time, hit-rate |

### 3.4 액션 타입 정의

| 액션 | 설명 | 파라미터 |
|------|------|----------|
| `highlight` | 노드/엣지 하이라이트 | nodes, edges, style, duration |
| `animate-edge` | 엣지 애니메이션 | edge, style, label, duration |
| `update-stat` | 통계 값 업데이트 | stat-id, value |
| `log` | 로그 메시지 출력 | message, type |
| `delay` | 대기 | duration |
| `conditional` | 조건 분기 | condition, then, else |
| `goto` | 다른 시나리오로 이동 | scenario |
| `parallel` | 병렬 실행 | steps[] |
| `reset` | 초기 상태로 리셋 | - |

### 3.5 표현식 문법

```yaml
# 변수 참조
{ $var: variableName }

# 랜덤 값
{ $random: { probability: 0.8 } }  # boolean
{ $random: { min: 1, max: 100 } }  # number

# 계산식
{ $add: [a, b, c] }
{ $multiply: [a, b] }
{ $subtract: [a, b] }

# 조건식
{ $if: { condition: ..., then: ..., else: ... } }
{ $eq: [a, b] }
{ $gt: [a, b] }
{ $and: [cond1, cond2] }
{ $or: [cond1, cond2] }
```

### 3.6 JSON Schema (핵심 부분)

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://animflow.dev/schema/v1.0/animflow.json",
  "title": "AnimFlow DSL Schema",
  "type": "object",
  "required": ["version", "nodes"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "metadata": {
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "author": { "type": "string" },
        "description": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } }
      }
    },
    "canvas": {
      "type": "object",
      "properties": {
        "width": { "type": "number", "default": 1200 },
        "height": { "type": "number", "default": 800 },
        "background": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "label", "position"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
          "type": { "enum": ["box", "circle", "icon", "database", "group"] },
          "label": { "type": "string" },
          "position": {
            "type": "object",
            "required": ["x", "y"],
            "properties": {
              "x": { "type": "number" },
              "y": { "type": "number" }
            }
          },
          "style": { "$ref": "#/definitions/nodeStyle" }
        }
      }
    },
    "edges": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "from", "to"],
        "properties": {
          "id": { "type": "string" },
          "from": { "type": "string" },
          "to": { "type": "string" },
          "label": { "type": "string" },
          "style": { "$ref": "#/definitions/edgeStyle" }
        }
      }
    },
    "scenarios": {
      "type": "array",
      "items": { "$ref": "#/definitions/scenario" }
    }
  },
  "definitions": {
    "nodeStyle": {
      "type": "object",
      "properties": {
        "color": { "type": "string" },
        "shape": { "enum": ["rect", "rounded-rect", "circle", "diamond"] },
        "width": { "type": "number" },
        "height": { "type": "number" }
      }
    },
    "edgeStyle": {
      "type": "object",
      "properties": {
        "color": { "type": "string" },
        "lineType": { "enum": ["solid", "dashed", "dotted"] },
        "lineWidth": { "type": "number", "default": 2 },
        "animated": { "type": "boolean", "default": false }
      }
    },
    "scenario": {
      "type": "object",
      "required": ["id", "steps"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "init": { "type": "object" },
        "steps": { "type": "array", "items": { "$ref": "#/definitions/step" } }
      }
    },
    "step": {
      "type": "object",
      "required": ["action"],
      "properties": {
        "action": {
          "enum": ["highlight", "animate-edge", "update-stat", "log",
                   "delay", "conditional", "goto", "parallel", "reset"]
        },
        "duration": { "type": "number", "default": 1000 }
      }
    }
  }
}
```

---

## 4. 기술 스택 제안

### 4.1 블로그 플랫폼

| 선택지 | 장점 | 추천도 |
|--------|------|--------|
| **Astro** | 정적 사이트 최적화, MDX 지원, 컴포넌트 자유도 | ⭐⭐⭐⭐⭐ |
| Next.js | 풀스택 기능, 동적 기능 강점 | ⭐⭐⭐⭐ |
| Docusaurus | 문서화 특화, 간편 설정 | ⭐⭐⭐ |

**추천: Astro + MDX**

### 4.2 AnimFlow 엔진 기술

```
┌─────────────────────────────────────────────┐
│              AnimFlow Engine                 │
├─────────────────────────────────────────────┤
│  렌더링: Canvas API (고성능) + SVG (접근성)  │
│  파서: yaml-js / JSON Schema Validator       │
│  에디터: Monaco Editor + Custom Language     │
│  애니메이션: Web Animations API / GSAP       │
│  UI 프레임워크: React / Solid.js             │
└─────────────────────────────────────────────┘
```

### 4.3 패키지 구조

```
@animflow/core          # DSL 파서, 엔진 코어
@animflow/renderer      # Canvas/SVG 렌더러
@animflow/editor        # Monaco 기반 에디터
@animflow/react         # React 컴포넌트
@animflow/astro         # Astro 통합 플러그인
@animflow/cli           # CLI 도구 (검증, 변환)
```

---

## 5. 단계별 구현 로드맵

### Phase 1: Foundation (MVP) - 핵심 목표: 동작하는 프로토타입

```
Week 1-2: DSL 스펙 & 파서
├── DSL 스펙 v0.1 문서화
├── JSON Schema 작성 및 검증기 구현
├── YAML/JSON 파서 구현 (js-yaml 사용)
└── 단위 테스트 작성

Week 3-4: 기본 렌더러
├── Canvas 2D 기반 렌더러
├── 노드 렌더링 (box 타입만)
├── 엣지 렌더링 (solid, 화살표)
└── 기본 애니메이션 (highlight, glow)

Week 5-6: 시나리오 엔진 기초
├── 시나리오 파서
├── 스텝 순차 실행
├── 기본 컨트롤 (시작/리셋 버튼)
└── 단일 HTML 데모 페이지
```

**Phase 1 결과물:**
- `@animflow/core` 패키지 v0.1.0
- 데모 HTML (`caching-flow-diagram.animflow.yaml` → 렌더링)
- DSL 스펙 문서

### Phase 2: Core Engine - 핵심 목표: 프로덕션 레벨 품질

```
Week 7-8: 노드/엣지 확장
├── 추가 노드 타입 (circle, icon, database, group)
├── 엣지 스타일 다양화 (dashed, dotted, animated)
├── 자동 레이아웃 알고리즘 (선택적)
└── 반응형 캔버스

Week 9-10: 시나리오 엔진 고도화
├── 변수 시스템 구현
├── 조건부 분기 (conditional)
├── 랜덤 값 표현식
├── 시나리오 간 이동 (goto)

Week 11-12: UI 컴포넌트
├── 통계 패널 컴포넌트
├── 로그 패널 컴포넌트
├── 범례 컴포넌트
├── 테마 시스템 기초
```

**Phase 2 결과물:**
- `@animflow/core` v0.5.0 (기능 완성)
- `@animflow/react` v0.1.0
- 3개 이상의 샘플 다이어그램

### Phase 3: Editor & Tooling - 핵심 목표: 개발자 경험 개선

```
Week 13-14: Monaco 에디터
├── Monaco Editor 통합
├── AnimFlow DSL 문법 하이라이팅
├── JSON Schema 기반 자동완성
└── 실시간 에러 표시

Week 15-16: 개발 도구
├── 실시간 프리뷰 (Split View)
├── CLI 도구 (검증, 변환)
├── VS Code 확장 기획
└── 디버깅 도구 (스텝 실행, 중단점)
```

**Phase 3 결과물:**
- `@animflow/editor` v0.1.0
- `@animflow/cli` v0.1.0
- 온라인 플레이그라운드

### Phase 4: Blog Platform - 핵심 목표: 실제 콘텐츠 제작 및 배포

```
Week 17-18: Astro 블로그 구축
├── Astro 프로젝트 설정
├── 블로그 테마 선택/커스터마이징
├── MDX 설정
└── AnimFlow 컴포넌트 통합

Week 19-20: 콘텐츠 제작
├── 캐싱 플로우 다이어그램 (기존 HTML 마이그레이션)
├── OAuth2 플로우 다이어그램
├── CI/CD 파이프라인 다이어그램
└── 블로그 포스트 3-5개 작성
```

**Phase 4 결과물:**
- `@animflow/astro` v0.1.0
- 운영 가능한 블로그 사이트
- 5개 이상의 AnimFlow 다이어그램 포스트

### Phase 5: Ecosystem - 핵심 목표: 커뮤니티 기반 성장

```
Week 21-24: 생태계 구축
├── 플러그인 시스템 설계 및 구현
├── 테마 시스템 (Dark/Light/Custom)
├── 커뮤니티 갤러리 웹사이트
├── 공식 문서 사이트
├── 튜토리얼 및 가이드
└── GitHub Organization 설정
```

**Phase 5 결과물:**
- 플러그인 API 및 샘플 플러그인
- 3개 이상의 테마
- 문서화 사이트 (animflow.dev)

### 로드맵 요약

```
     Phase 1        Phase 2        Phase 3        Phase 4        Phase 5
   Foundation     Core Engine      Tooling         Blog        Ecosystem
 ──────────────┬──────────────┬──────────────┬──────────────┬──────────────
    Week 1-6   │   Week 7-12  │  Week 13-16  │  Week 17-20  │  Week 21-24
               │              │              │              │
   - DSL v0.1  │ - Node types │ - Monaco     │ - Astro blog │ - Plugins
   - Parser    │ - Variables  │ - CLI        │ - 5+ posts   │ - Themes
   - Canvas    │ - Conditions │ - Preview    │ - MDX        │ - Gallery
   - Demo HTML │ - Stats/Logs │ - Debug      │ - Deploy     │ - Docs
               │              │              │              │
       ▼              ▼              ▼              ▼              ▼
   v0.1.0        v0.5.0         v1.0-beta      v1.0.0         v1.5.0
```

---

## 6. 블로그 통합 예시

### 6.1 MDX 파일에서 사용

```mdx
---
title: "멀티 레이어 캐싱 이해하기"
date: 2025-01-15
---

# 멀티 레이어 캐싱 시스템

캐싱은 시스템 성능의 핵심입니다. 아래 인터랙티브 다이어그램으로
각 시나리오를 직접 체험해보세요.

<AnimFlow
  src="./diagrams/caching-flow.animflow.yaml"
  height={800}
  defaultScenario="random"
/>

## 주요 포인트

1. **L1 캐시 (Gocache)**: 가장 빠른 로컬 캐시
2. **L2 캐시 (Infinispan)**: 분산 캐시 레이어
3. **원본 소스 (LDAP)**: 최종 데이터 소스
```

### 6.2 Astro 컴포넌트

```astro
---
// src/components/AnimFlowDiagram.astro
import { AnimFlow } from '@animflow/astro';
const { src, height = 600 } = Astro.props;
---

<div class="animflow-container">
  <AnimFlow src={src} height={height} client:load />
</div>

<style>
  .animflow-container {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    margin: 2rem 0;
  }
</style>
```

---

## 7. 표준화 및 오픈소스 전략

### 7.1 표준 스펙 문서
- AnimFlow Specification v1.0
- JSON Schema 정의
- 호환성 가이드라인

### 7.2 오픈소스 전략
```
GitHub Organization: animflow
├── animflow-spec        # 스펙 문서
├── animflow-core        # 코어 엔진
├── animflow-editor      # 에디터
├── animflow-integrations # 플랫폼 통합
└── animflow-examples    # 예제 갤러리
```

### 7.3 커뮤니티 빌딩
- 예제 갤러리 운영
- 기여자 가이드
- Discord/Slack 커뮤니티

---

## 8. 블로그 콘텐츠 아이디어

AnimFlow로 표현하기 좋은 주제들:

| 카테고리 | 콘텐츠 아이디어 |
|----------|----------------|
| **시스템 설계** | 마이크로서비스 통신, 이벤트 소싱, CQRS |
| **인프라** | K8s 배포 플로우, CI/CD 파이프라인, 서비스 메시 |
| **알고리즘** | 정렬 알고리즘, 트리 순회, 그래프 탐색 |
| **네트워크** | TCP 핸드셰이크, TLS 협상, DNS 조회 |
| **데이터** | 트랜잭션 격리 레벨, 복제 전략, 샤딩 |
| **보안** | OAuth2 플로우, JWT 검증, Zero Trust |

---

## 9. 예상 결과물

### 9.1 단기 (3개월)
- AnimFlow DSL v0.1 스펙
- 기본 렌더러 및 에디터
- 블로그 MVP (3-5개 포스트)

### 9.2 중기 (6개월)
- AnimFlow v1.0 릴리즈
- Astro/Next.js 플러그인
- 20+ 블로그 포스트 및 다이어그램

### 9.3 장기 (12개월)
- 커뮤니티 기반 에코시스템
- 표준 채택 확산
- 기술 블로그 브랜딩 확립

---

## 10. 리스크 분석 및 대응 방안

| 리스크 | 영향도 | 발생확률 | 대응 방안 |
|--------|--------|----------|-----------|
| DSL 설계 복잡도 증가 | 높음 | 중간 | MVP 범위를 최소화하고 점진적 확장 |
| 브라우저 호환성 이슈 | 중간 | 낮음 | Canvas 2D API는 범용 지원, polyfill 준비 |
| 성능 문제 (대규모 노드) | 중간 | 중간 | 가상화, WebGL 렌더러 옵션 제공 |
| 커뮤니티 채택 실패 | 높음 | 중간 | 블로그 콘텐츠로 먼저 가치 증명 |
| 경쟁 도구 출현 | 중간 | 낮음 | 오픈소스로 커뮤니티 기반 차별화 |
| 개발 리소스 부족 | 높음 | 높음 | Phase별 명확한 스코프, 외부 기여 활용 |

### 리스크 완화 전략

1. **MVP 집중**: Phase 1에서 핵심 기능만 구현, 복잡한 기능은 Phase 2+로 연기
2. **프로토타입 활용**: 기존 HTML 프로토타입을 테스트베드로 활용
3. **문서화 우선**: DSL 스펙을 먼저 완성하여 구현 방향 명확화
4. **피드백 루프**: 블로그 콘텐츠를 통해 실제 사용 피드백 조기 확보

---

## 11. 경쟁 분석 및 차별화

| 도구 | 특징 | AnimFlow 차별점 |
|------|------|-----------------|
| **Mermaid** | 텍스트 기반 정적 다이어그램 | 애니메이션, 인터랙션, 시나리오 |
| **D3.js** | 데이터 시각화 라이브러리 | 선언적 DSL, 코딩 불필요 |
| **Excalidraw** | 손그림 스타일 드로잉 | 시스템 아키텍처 특화, 애니메이션 |
| **Whimsical** | 협업 다이어그램 도구 (SaaS) | 오픈소스, 자체 호스팅, DSL 기반 |
| **Motion Canvas** | 코드 기반 애니메이션 | DSL로 간소화, 비개발자 접근성 |

### AnimFlow의 고유 가치

```
┌─────────────────────────────────────────────────────────────────┐
│                    AnimFlow Unique Value                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│    선언적 DSL    ←→    시나리오 기반    ←→    실시간 통계        │
│         │                    │                    │               │
│    YAML/JSON         조건부 분기           성능 시각화           │
│    스키마 검증        변수 시스템           로그 추적             │
│                                                                   │
│                           ↓                                       │
│              ┌─────────────────────────┐                         │
│              │   기술 교육의 새로운     │                         │
│              │     표현 방식 제시       │                         │
│              └─────────────────────────┘                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 12. 즉시 시작 가능한 작업 (Quick Start)

### Day 1-3: 프로젝트 초기화
```bash
# 1. 모노레포 설정
mkdir animflow && cd animflow
npm init -y
npx lerna init

# 2. 패키지 구조
packages/
├── core/         # @animflow/core
├── react/        # @animflow/react
├── cli/          # @animflow/cli
└── playground/   # 데모 사이트

# 3. 기본 의존성
npm install typescript yaml ajv canvas
```

### Day 4-7: 첫 번째 마일스톤
1. `animflow.schema.json` 작성 (JSON Schema)
2. `caching-flow.animflow.yaml` 작성 (기존 HTML → DSL 변환)
3. 간단한 파서 + Canvas 렌더러 프로토타입
4. "Hello World" 수준의 동작 확인

### 검증 질문 (Definition of Done)
- [ ] YAML 파일을 파싱할 수 있는가?
- [ ] 노드와 엣지가 Canvas에 렌더링 되는가?
- [ ] 시작 버튼을 누르면 애니메이션이 시작되는가?

---

## 13. 결론

이 프로젝트는 단순한 블로그를 넘어 **기술 시각화의 새로운 표준**을 제시합니다.

### 핵심 가치

| 가치 | 설명 | 기대 효과 |
|------|------|----------|
| **선언적** | 코드가 아닌 정의로 다이어그램 생성 | 비개발자도 접근 가능 |
| **인터랙티브** | 정적 이미지를 넘어선 동적 체험 | 복잡한 개념 이해도 향상 |
| **재사용** | 컴포넌트화된 다이어그램 | 생산성 증가 |
| **오픈소스** | 커뮤니티 기반 발전 | 지속가능한 성장 |

### 성공을 위한 핵심 원칙

```
1. MVP First    - 작게 시작, 빠르게 검증
2. Eat Own Food - 자체 블로그로 먼저 사용
3. Open by Default - 처음부터 오픈소스
4. Docs Driven  - 스펙 문서화 우선
```

### 비전 선언

```
"복잡한 기술을 직관적인 애니메이션으로,
 누구나 이해할 수 있는 시각적 경험을 제공한다."
```

---

## 부록

### A. 관련 리소스
- Canvas API: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API
- YAML 스펙: https://yaml.org/spec/
- JSON Schema: https://json-schema.org/
- Astro 문서: https://docs.astro.build/

### B. 영감을 받은 프로젝트
- Mermaid.js
- Motion Canvas
- Excalidraw
- D3.js

### C. 변경 이력
| 버전 | 날짜 | 변경 사항 |
|------|------|----------|
| 1.0 | 2025-11-26 | 초안 작성 |
| 1.1 | 2025-11-26 | DSL 스펙 상세화, 로드맵 구체화, 리스크 분석 추가 |

---

*이 문서는 tech-blog 프로젝트의 구현 제안서입니다.*
*작성일: 2025-11-26 | 최종 수정: 2025-11-26*
